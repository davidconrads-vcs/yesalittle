import { ContentPage } from '../types'
import { page as spainRestaurant } from './spain-restaurant'

// Every page the generator builds. Add new page modules here.
export const pages: ContentPage[] = [spainRestaurant]
